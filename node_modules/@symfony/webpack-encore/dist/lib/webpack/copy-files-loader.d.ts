import type { LoaderContext } from 'webpack';
interface CopyFilesLoaderOptions {
    context: string;
    filename: string;
    regExp?: RegExp;
    patternSource: string;
    patternFlags: string;
}
export declare const raw = true;
/**
 * A webpack loader that copies files to the output directory.
 *
 * This loader replaces the deprecated file-loader by using webpack's
 * native this.emitFile() API. It supports filtering files by pattern
 * and customizing output paths using template placeholders.
 *
 * @param source The raw file content
 * @returns An ESM module that exports the public URL of the copied file
 */
export default function loader(this: LoaderContext<CopyFilesLoaderOptions>, source: Buffer): string;
export {};
