{% extends 'emails/base.html.twig' %}

{% block title %}
    {% if action == 'reactivated' %}Organisation réactivée{% else %}Organisation suspendue{% endif %}
{% endblock %}

{% block body %}
    {% apply inline_css(source('@styles/email.css')) %}
        <div class="email-wrapper">
            <div class="email-content">
                <div class="email-header">
                    <img src="{{ logo_cid }}" alt="OnlineDIAB" class="logo">
                </div>
                <div class="email-body">
                    <h2>
                        {% if action == 'reactivated' %}
                            Votre organisation a été réactivée
                        {% else %}
                            Votre organisation a été suspendue
                        {% endif %}
                    </h2>

                    <p>Bonjour {{ user.fullName }},</p>

                    {% if action == 'reactivated' %}
                        <p>
                            L'accès à la plateforme OnlineDIAB pour l'organisation
                            <strong>{{ organization.name }}</strong> a été rétabli.
                            Tous les membres peuvent de nouveau se connecter.
                        </p>
                    {% else %}
                        <p>
                            L'organisation <strong>{{ organization.name }}</strong> a été suspendue
                            {% if endsAt %}<strong>jusqu'au {{ endsAt|date('d/m/Y') }}</strong>{% else %}<strong>jusqu'à nouvel ordre</strong>{% endif %}.
                            Vos accès et ceux de tous les membres sont momentanément bloqués.
                        </p>
                        {% if reason %}
                            <div class="quote">
                                <strong>Motif :</strong> {{ reason }}
                            </div>
                        {% endif %}
                        <p>
                            Si vous pensez qu'il s'agit d'une erreur, veuillez contacter
                            le super administrateur de la plateforme.
                        </p>
                    {% endif %}

                    <div class="btn-container">
                        <a href="{{ appUrl }}" class="btn-reset" target="_blank">
                            Accéder à la plateforme
                        </a>
                    </div>
                </div>
                <div class="email-footer">
                    <p>&copy; {{ "now"|date("Y") }} OnlineDIAB. Tous droits réservés.</p>
                    <p><a href="https://www.onlinediab.com">www.onlinediab.com</a></p>
                </div>
            </div>
        </div>
    {% endapply %}
{% endblock %}